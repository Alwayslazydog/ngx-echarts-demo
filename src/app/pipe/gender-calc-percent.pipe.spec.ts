import { GenderCalcPercentPipe } from './gender-calc-percent.pipe';

describe('GenderCalcPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderCalcPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
